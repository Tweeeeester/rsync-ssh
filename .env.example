# Connection
NAS_HOST=192.168.1.100
NAS_USER=backup
NAS_PORT=22

# Paths inside the container
SSH_KEY_PATH=/root/.ssh/id_rsa
SYNC_PAIRS_FILE=/config/sync-pairs.txt

# Cron schedule (default: every hour on the hour)
# See https://crontab.guru for help with the format
CRON_SCHEDULE=0 * * * *
